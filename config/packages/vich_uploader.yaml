vich_uploader:
    db_driver: orm

    mappings:
        evenement_image:
            uri_prefix: /images/evenements
            upload_destination: '%kernel.project_dir%/public/images/evenements'
            namer: Vich\UploaderBundle\Naming\UniqidNamer    # si on veut changer notre image, cette ligne permet de supprimer l'ancienne (et de la remplacer)

    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
